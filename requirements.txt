# Direct dependencies. Tested on Python 3.14 (macOS, arm64) and 3.11+.
streamlit>=1.38
numpy>=2.0
pandas>=2.2
scikit-image>=0.24
scipy>=1.13
opencv-python-headless>=4.10
imageio>=2.34
tifffile>=2024.5.10
pillow>=10.3
matplotlib>=3.9

# Needed only by scripts/make_sample_data.py, to fetch the public reference
# images once. The committed PNGs mean the app itself never needs it.
pooch>=1.8

# Testing
pytest>=8.0
